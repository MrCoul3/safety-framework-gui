import React from 'react';
import {observer} from "mobx-react-lite";
import style from './style.module.css';

interface IBarrierPassportsBoard {
    
}

const BarrierPassportsBoard = observer((props: IBarrierPassportsBoard) => {
    return (
        <div className={style.BarrierPassportsBoard}>
            
        </div>
    );
});

export default BarrierPassportsBoard;